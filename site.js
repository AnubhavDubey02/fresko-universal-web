const menu=document.querySelector('.menu');const links=document.querySelector('.navlinks');if(menu&&links)menu.addEventListener('click',()=>links.classList.toggle('open'));
document.querySelectorAll('[data-year]').forEach(x=>x.textContent=new Date().getFullYear());
const leadForm=document.querySelector('#lead-form');if(leadForm){leadForm.addEventListener('submit',e=>{e.preventDefault();const fd=new FormData(leadForm);const s=encodeURIComponent(`Trade enquiry: ${fd.get('type')||'General'} — ${fd.get('company')||fd.get('name')||'Website lead'}`);const b=encodeURIComponent(`Name: ${fd.get('name')||''}\nCompany: ${fd.get('company')||''}\nCountry/City: ${fd.get('location')||''}\nPhone/WhatsApp: ${fd.get('phone')||''}\nEmail: ${fd.get('email')||''}\nRequirement: ${fd.get('message')||''}`);location.href=`mailto:freskouniversal@zohomail.com?subject=${s}&body=${b}`})}
